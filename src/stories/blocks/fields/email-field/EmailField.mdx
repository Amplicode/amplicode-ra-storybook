import { Meta, Story, Primary, Controls } from '@storybook/blocks';

import * as EmailFieldStories from './EmailField.stories';

<Meta of={EmailFieldStories} />

# EmailField

`<EmailField>` displays an email as a Material UI’s `<Link href="mailto:" />` component.

<Controls />

## Default

<Story of={EmailFieldStories.Default} />