import { Meta, Story, Primary, Controls } from '@storybook/blocks';

import * as FileFieldStories from './FileField.stories';

<Meta of={FileFieldStories} />

# FileField

If you need to render a link to a file based on a path contained in a record field, you can use the `<FileField />` component. [More information](https://marmelab.com/react-admin/FileField.html).

<Controls />

## Default

<Story of={FileFieldStories.Default} />