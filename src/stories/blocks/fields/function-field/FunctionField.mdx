import { Meta, Story, Primary, Controls } from '@storybook/blocks';

import * as FunctionFieldStories from './FunctionField.stories';

<Meta of={FunctionFieldStories} />

# FunctionField

If you need a special function to render a field, `<FunctionField>` is the perfect match. It executes a render function using the current record as parameter. [More information](https://marmelab.com/react-admin/FunctionField.html).

<Controls />

## Default

<Story of={FunctionFieldStories.Default} />
